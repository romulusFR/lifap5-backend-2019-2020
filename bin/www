'use strict'; 
// #!/usr/bin/env node

const {logger, http_port, env, name, version} = require('../utils/');
const app = require('../app');

app.listen(http_port, () => {
  logger.info(`${name}@${version}[${env}] running on ${http_port}`);
});