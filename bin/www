// #!/usr/bin/env node

const { logger, config} = require('../utils/');
const app = require('../app');

app.listen(config.httpPort, () => {
  logger.info(`${config.name}@${config.version}[${config.env}] running on ${config.httpPort}`);
});
