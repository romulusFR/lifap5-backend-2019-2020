#!/usr/bin/env node

const { logger, httpPort, env, name, version } = require('../utils/');
const app = require('../app');

app.listen(httpPort, () => {
  logger.info(`${name}@${version}[${env}] running on ${httpPort}`);
});
