'use strict'; 
// #!/usr/bin/env node

const {logger, http_port, environment} = require('../utils/');
const app = require('../app');

app.listen(http_port, () => {
  logger.info(`Running on port ${http_port} with env '${environment}'`);
});